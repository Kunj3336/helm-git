apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "neural-template.fullname" . }}-test-connection"
  labels:
    {{- include "neural-template.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "neural-template.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
