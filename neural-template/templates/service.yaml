apiVersion: v1
kind: Service
metadata:
  name: {{ include "neural-template.fullname" . }}
  labels:
    {{- include "neural-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "neural-template.selectorLabels" . | nindent 4 }}
