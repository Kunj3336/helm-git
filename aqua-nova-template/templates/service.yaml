apiVersion: v1
kind: Service
metadata:
  name: {{ include "aqua-nova-template.fullname" . }}
  labels:
    {{- include "aqua-nova-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aqua-nova-template.selectorLabels" . | nindent 4 }}
